# Python
__pycache__/
*.pyc
.pytest_cache/

# Supervisor runtime artifacts (owned by Supervisor, never committed)
.supervisor/

# Live config — source tracks only the example template
supervisor.toml

# Agent runtime state — never tracked in the Supervisor source repo.
# See docs/supervisor-protocol.md §0 (Source vs Runtime).
.agent/
.commandcode

# Nested target repos used as fixtures
hello_world/

# Editors / OS
*.swp
.DS_Store
